app:
  name: bff
  image:
    repository: "onecx/onecx-iam-bff"
  operator:
    # Permission
    permission:
      enabled: true
      spec:
        permissions:
          user:
            read: permission on all GET requests and POST search
          role:
            read: permission on all GET requests and POST search